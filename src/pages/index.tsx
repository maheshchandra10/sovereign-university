import { type NextPage } from "next";

const Home: NextPage = () => {
  return (
    <main className="w-50 bg- flex min-h-screen flex-col items-center justify-center"></main>
  );
};

export default Home;
